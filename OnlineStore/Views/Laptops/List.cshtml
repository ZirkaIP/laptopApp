@model LaptopListViewModels

<h1>All products</h1>

<h3>@Model.currCategory</h3>

<div class="row mt-5 mb-2 ">
    @{
        foreach (Laptop laptop in Model.AllLaptops)
        {
            @Html.Partial("AllLaptops", laptop)
        }
    }
</div>
